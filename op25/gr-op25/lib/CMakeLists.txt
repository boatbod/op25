# Copyright 2011,2012,2016,2018,2019 Free Software Foundation, Inc.
#
# This file was generated by gr_modtool, a tool from the GNU Radio framework
# This file is a part of gr-op25
#
# SPDX-License-Identifier: GPL-3.0-or-later
#

########################################################################
# Setup library
########################################################################
include(GrPlatform) #define LIB_SUFFIX

list(APPEND op25_sources
    fsk4_demod_ff_impl.cc
    fsk4_slicer_fb_impl.cc
    decoder_bf_impl.cc
    decoder_ff_impl.cc
    abstract_data_unit.cc
    data_unit.cc
    hdu.cc
    ldu1.cc
    ldu2.cc
    pdu.cc
    tdu.cc
    tsbk.cc
    data_unit_handler.cc
    logfile_du_handler.cc
    p25cai_du_handler.cc
    snapshot_du_handler.cc
    imbe_decoder.cc
    imbe_decoder_factory.cc
    dummy_imbe_decoder.cc
    offline_imbe_decoder.cc
    voice_data_unit.cc
    voice_du_handler.cc
    pcap_source_b_impl.cc
    software_imbe_decoder.cc
    vc55_imbe_decoder.cc
    value_string.cc
    pickle.cc
    pcap_source_b_impl.cc
    message.cc
    msg_queue.cc
    msg_handler.cc
)

set(op25_sources "${op25_sources}" PARENT_SCOPE)
if(NOT op25_sources)
    MESSAGE(STATUS "No C++ sources... skipping lib/")
    return()
endif(NOT op25_sources)

add_library(gnuradio-op25 SHARED ${op25_sources})

target_link_libraries(gnuradio-op25 gnuradio::gnuradio-runtime itpp pcap)
target_include_directories(gnuradio-op25
    PUBLIC $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/../include>
    PUBLIC $<INSTALL_INTERFACE:include>
    PRIVATE "${ITPP_INCLUDE_DIR}"
    PRIVATE "${PCAP_INCLUDE_DIR}"
  )
set_target_properties(gnuradio-op25 PROPERTIES DEFINE_SYMBOL "gnuradio_op25_EXPORTS")

if(APPLE)
    set_target_properties(gnuradio-op25 PROPERTIES
        INSTALL_NAME_DIR "${CMAKE_INSTALL_PREFIX}/lib"
    )
endif(APPLE)

########################################################################
# Install built library files
########################################################################
include(GrMiscUtils)
GR_LIBRARY_FOO(gnuradio-op25)

########################################################################
# Print summary
########################################################################
message(STATUS "Using install prefix: ${CMAKE_INSTALL_PREFIX}")
message(STATUS "Building for version: ${VERSION} / ${LIBVER}")

########################################################################
# Build and register unit test
########################################################################
include(GrTest)

# If your unit tests require special include paths, add them here
#include_directories(${CPPUNIT_INCLUDE_DIRS}))
# List all files that contain Boost.UTF unit tests here
list(APPEND test_op25_sources
)
# Anything we need to link to for the unit tests go here
list(APPEND GR_TEST_TARGET_DEPS gnuradio-op25)

if(NOT test_op25_sources)
    MESSAGE(STATUS "No C++ unit tests... skipping")
    return()
endif(NOT test_op25_sources)

foreach(qa_file ${test_op25_sources})
    GR_ADD_CPP_TEST("op25_${qa_file}"
        ${CMAKE_CURRENT_SOURCE_DIR}/${qa_file}
    )
endforeach(qa_file)

add_executable(test-op25 ${test_op25_sources})

target_link_libraries(
  test-op25
  ${GNURADIO_RUNTIME_LIBRARIES}
  ${Boost_LIBRARIES}
  ${CPPUNIT_LIBRARIES}
  gnuradio-op25
)

GR_ADD_TEST(test_op25 test-op25)
