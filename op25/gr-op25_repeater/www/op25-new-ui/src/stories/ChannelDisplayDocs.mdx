import { Meta, Story, Preview, Canvas } from "@storybook/addon-docs";
import ChannelDisplay from "components/ChannelDisplay";

<Meta title="OP25/Channel Display" component={ChannelDisplay} />

# Channel Display

With `OP25`, we can define a story for `ChannelDisplay` right in the middle of our
Markdown documentation.

<Story id="op25-channel-display--dark-theme" />
